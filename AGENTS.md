# Firebase 프로필 운영 규칙

- 프로필 공개 데이터는 Firestore `siteContent/profile` 문서에 저장한다. 필드는 `name`, `status`, `info`, `imageUrl`을 사용한다.
- 웹에서 쓰기 권한을 판단하거나 부여하지 않는다. Google 로그인 후 Firestore `admins/{uid}` 문서의 `enabled: true`일 때만 편집 UI를 보여 준다.
- Firestore 보안 규칙도 반드시 같은 검사를 수행해야 한다. `firestore.rules`의 관리자 전용 쓰기 규칙을 완화하지 않는다.
- 관리자 등록은 Firebase Console 또는 신뢰된 Admin SDK 환경에서만 한다. 브라우저 코드, 저장소, 클라이언트 설정에 서비스 계정 키나 비밀 값을 넣지 않는다.
- Firebase 웹 설정은 `firebase-config.js`에 넣는다. 이 파일의 공개 웹 설정은 배포 가능하지만, 실제 접근 통제는 Authentication과 Firestore 보안 규칙으로 한다.

## 추가 보안 규칙

- 정적 웹사이트의 HTML·CSS·JavaScript·빌드 산출물·브라우저 저장소(LocalStorage, SessionStorage, IndexedDB, 쿠키)에 넣은 값은 비밀이 아니다. 난독화, Base64 인코딩, 환경 변수 치환, private Git 저장소에서의 프런트엔드 빌드도 비밀 보호 수단으로 취급하지 않는다.
- "관리자 비밀번호"를 정적 페이지에 두는 방식은 사용하지 않으며, 관리자 인증은 Firebase Authentication의 Google 로그인으로만 처리한다. 정적 산출물에는 평문 비밀번호, 검증용 해시, 마스터키, 장기 세션 서명 키를 포함하지 않는다.
- 비밀번호 기반 **인증·권한 부여**가 필요하면 인증·비밀번호 검증·세션 발급은 신뢰된 서버 또는 Firebase Authentication에서 처리한다. 아래의 정적 콘텐츠 암호화 예외는 인증이나 접근 제어 수단으로 취급하지 않는다.

## 정적 콘텐츠 암호화 예외

- 게시판·로그처럼 정적 배포해야 하는 콘텐츠에는, 공유 비밀번호를 복호화 키로 사용하는 클라이언트 측 암호화를 허용한다. 이는 비밀번호를 아는 사람에게만 읽기 쉽게 만드는 콘텐츠 보호 방식이며, Firestore 보안 규칙이나 서버 인증을 대체하지 않는다.
- 보호 대상의 평문은 빌드 단계에서 AES-256-GCM으로 암호화한다. 배포 산출물에는 암호문, salt, IV, 인증 태그만 둘 수 있으며, 평문 콘텐츠·비밀번호 목록·비밀번호 검증값·마스터키는 절대 포함하지 않는다.
- 비밀번호 목록을 빌드 중에 쓰는 경우에는 별도의 암호문 파일로만 저장하고, 복호화용 마스터키는 CI/CD 시크릿 또는 로컬 비밀 저장소에서 `BOARD_CONTENT_MASTER_KEY`처럼 환경 변수로만 제공한다. 마스터키는 브라우저, 저장소 소스, 빌드 산출물, 로그에 노출하지 않는다.
- 브라우저에서는 사용자가 방금 입력한 게시판 비밀번호로 PBKDF2(충분한 반복 횟수) 키를 유도해 암호문을 직접 복호화한다. 비밀번호를 LocalStorage·SessionStorage·IndexedDB·쿠키에 저장하거나, 네트워크로 전송하거나, 로그·오류 메시지에 기록하지 않는다. 별도 "비밀번호 비교" API를 만들지 않으며, AES-GCM 복호화 성공 여부로만 열람을 판단한다.
- Firebase 관리자 UI의 게시판 등록·수정 작업에 한해, 관리자가 직접 입력한 fine-grained GitHub 토큰과 `BOARD_CONTENT_MASTER_KEY`를 요청 처리 중 메모리에서만 사용할 수 있다. 이 예외는 `myweb-private`의 Contents API 호출과 암호화된 비밀번호 목록의 갱신에만 적용한다. 두 값은 입력 완료 직후 비우며 브라우저 저장소·소스·배포물·로그·오류 메시지에 저장하거나 표시하지 않는다.
- 이 방식은 암호문을 내려받은 사람이 오프라인에서 비밀번호를 반복 추측하는 것을 막지 못하며, 비밀번호를 안 사람의 재공유·즉시 권한 철회·요청 제한도 제공하지 않는다. 따라서 각 게시판에는 길고 무작위적인 별도 비밀번호를 사용하고, 강한 접근 통제·철회·감사가 필요하면 신뢰된 서버 기반 방식으로 전환한다.
- private 저장소는 소스 코드 접근을 제한하는 용도로만 사용하고, 정적 사이트가 런타임에 private 저장소에서 비밀을 읽도록 만들지 않는다. 브라우저에 저장소 토큰, deploy key, GitHub App 비밀키를 넣는 순간 누구나 추출할 수 있다.
- 비밀값은 코드 저장소와 분리된 Secret Manager, CI/CD 시크릿 또는 로컬 비밀 저장소에만 보관한다. 배포 과정에서 정말 필요한 서버 측 비밀만 주입하며, 정적 산출물·소스맵·로그·캐시·오류 보고서에 포함되지 않는지 확인한다.
- Firebase 웹 API 키는 공개 식별자일 수 있으나, Google Cloud Console에서 도메인(HTTP referrer)과 필요한 API로 제한한다. 제한은 보조 방어이며, 데이터 권한은 반드시 Authentication과 Firestore 보안 규칙으로 강제한다.
- 관리자 로그인 후에도 권한을 브라우저에 캐시해 신뢰하지 않는다. 민감한 Firestore 요청마다 규칙에서 `request.auth.uid`와 `admins/{uid}.enabled == true`를 재검증하고, 권한 철회는 즉시 반영되도록 한다.
- `admins/{uid}`의 `enabled` 외에 역할, 이메일, 권한 목록처럼 확장 가능한 권한 정보를 클라이언트가 작성하게 하지 않는다. 권한 변경은 Firebase Console 또는 Admin SDK를 사용하는 별도 신뢰 환경의 제한된 운영 절차로만 수행한다.
- Firestore 규칙에서는 `get()` 대상 관리자 문서의 존재·필드·불리언 값을 안전하게 검사하고, 규칙 오류가 권한 허용으로 이어지지 않게 기본 거부(deny by default)를 유지한다.
- 프로필 이미지 업로드 기능을 추가할 경우 파일 확장자나 브라우저가 보내는 MIME 타입만 신뢰하지 않는다. Storage 규칙에서 인증·관리자 여부·콘텐츠 타입·용량을 제한하고, 공개 URL과 업로드 경로를 사용자 입력으로 임의 지정하게 하지 않는다.
- Content Security Policy를 설정해 스크립트·이미지·연결 출처를 필요한 도메인으로 한정하고, 인라인 스크립트·`eval`·불필요한 서드파티 스크립트를 금지한다. `firebase-config.js`와 Firebase에 필요한 출처만 예외로 명시한다.
- 클라이언트의 로그인 상태, 관리자 UI 노출 여부, JavaScript 조건문은 보안 경계가 아니다. 모든 민감한 읽기·쓰기 권한은 Firestore 보안 규칙 또는 신뢰된 서버 환경에서 검증한다.
- `admins/{uid}` 문서는 일반 사용자가 읽거나 생성·수정·삭제할 수 없게 한다. 관리자 권한 여부 확인에 필요한 최소 범위만 읽도록 보안 규칙을 설계한다.
- `siteContent/profile`에 대한 쓰기는 허용된 필드(`name`, `status`, `info`, `imageUrl`)만 포함하고, 각 값의 타입·최대 길이·URL 형식을 보안 규칙에서 검증한다. 예상하지 못한 필드와 서버 관리 필드의 클라이언트 쓰기는 거부한다.
- 공개 읽기가 필요한 문서만 명시적으로 공개한다. 컬렉션 또는 데이터베이스 전체에 적용되는 포괄적 `allow read, write` 규칙은 사용하지 않는다.
- 화면에 Firestore 값을 표시할 때 사용자 제공 문자열을 `innerHTML`로 삽입하지 않는다. `textContent`를 사용하고, 이미지 URL은 `https:` 등 허용한 스킴과 신뢰 가능한 형식만 사용한다.
- Firebase API 키, 프로젝트 ID 등 공개 웹 설정과 서비스 계정 키·관리자 SDK 자격 증명·토큰을 구분한다. 후자는 `.env`, 로컬 비밀 저장소 또는 CI의 시크릿으로만 관리하고, 커밋·로그·오류 메시지에 노출하지 않는다.
- 보안 규칙 변경은 배포 전에 Emulator 또는 테스트로 비관리자 쓰기 거부와 관리자 쓰기 허용을 모두 확인한다. 프로덕션 규칙을 임시로 완화해 문제를 해결하지 않는다.
- 의존성 추가와 CDN 스크립트 사용 시 출처와 버전을 검토한다. 가능하면 버전을 고정하고, 무결성 검증을 지원하는 정적 외부 리소스에는 SRI를 적용한다.
- 관리자 기능에서 수집한 오류와 감사 정보에는 개인정보, 인증 토큰, Firestore 전체 문서 내용을 기록하지 않는다. 필요한 경우에도 최소한의 식별자와 작업 결과만 남긴다.
- 커밋 전과 배포 전에는 비밀 스캔을 수행하고, `.env`, 서비스 계정 JSON, 개인 키, 토큰, 백업 파일, 빌드 캐시를 `.gitignore`에 등록한다. 한 번이라도 원격 저장소에 노출된 비밀은 삭제 커밋만으로 해결하지 말고 즉시 폐기·교체한다.
- 보안 관련 설정 변경에는 최소 권한 원칙과 변경 이력을 적용한다. 운영 계정은 다중 인증을 사용하고, Firebase·Google Cloud·저장소의 관리자 권한을 정기적으로 검토한다.
